diff --git a/cn_clip/clip/utils.py b/cn_clip/clip/utils.py
index 0b7367a..b81ad08 100644
--- a/cn_clip/clip/utils.py
+++ b/cn_clip/clip/utils.py
@@ -166,6 +166,10 @@ def image_transform(image_size=224):
 
 def create_model(model_name, checkpoint=None):
     vision_model, text_model = model_name.split('@')
+
+
+
+    
     # Initialize the model.
     vision_model_config_file = Path(
         __file__).parent / f"model_configs/{vision_model.replace('/', '-')}.json"
diff --git a/cn_clip/deploy/pytorch_to_onnx.py b/cn_clip/deploy/pytorch_to_onnx.py
index ceed11a..19ebc0e 100644
--- a/cn_clip/deploy/pytorch_to_onnx.py
+++ b/cn_clip/deploy/pytorch_to_onnx.py
@@ -2,7 +2,7 @@
 """
 This script converts PyTorch implemented Chinese-CLIP (text or vision) model to ONNX format for CPU/GPU deployment.
 """
-
+import sys
 import os
 import argparse
 from PIL import Image
@@ -10,8 +10,10 @@ import torch
 import torch.onnx
 from onnx import load_model, save_model
 from onnxmltools.utils import convert_float_to_float16
+
+sys.path.append('./Chinese-CLIP1')
 import cn_clip.clip as clip
-from clip.utils import _MODELS, _MODEL_INFO, _download, available_models, create_model, image_transform
+from cn_clip.clip.utils import _MODELS, _MODEL_INFO, _download, available_models, create_model, image_transform
 
 def parse_args():
     parser = argparse.ArgumentParser()
@@ -118,7 +120,9 @@ if __name__ == '__main__':
                     input_names=['text'],
                     output_names=['unnorm_text_features'],
                     export_params=True,
-                    opset_version=13,
+                    dynamic_axes=None,
+                    #opset_version=13,
+                    opset_version=14,
                     verbose=True)
         # convert text FP16 ONNX model based on the FP32 model
         text_fp16_onnx_path = f"{args.save_onnx_path}.txt.fp16.onnx"
@@ -143,7 +147,9 @@ if __name__ == '__main__':
                     output_names=['unnorm_image_features'],
                     export_params=True,
                     do_constant_folding=False,
-                    opset_version=13,
+                    dynamic_axes=None,
+                    #opset_version=13,
+                    opset_version=14,
                     verbose=True)
         # for ViT-H-14 FP32 model, make another conversion to deal with the generated small files
         if args.model_arch == "ViT-H-14":
diff --git a/cn_clip/eval/zeroshot_evaluation.py b/cn_clip/eval/zeroshot_evaluation.py
index 1b7da83..76d9d48 100644
--- a/cn_clip/eval/zeroshot_evaluation.py
+++ b/cn_clip/eval/zeroshot_evaluation.py
@@ -10,6 +10,9 @@ import json
 from tqdm import tqdm
 
 import torch
+torch.multiprocessing.set_sharing_strategy('file_system')
+
+import numpy as np
 
 from cn_clip.clip.model import convert_weights, CLIP
 from cn_clip.clip import tokenize
@@ -23,6 +26,7 @@ from cn_clip.eval.cvinw_zeroshot_templates import (
     aircraft_templates,
     eurosat_templates,
     country211_templates,
+    cifar100_templates,
 )
 
 
@@ -82,12 +86,12 @@ def parse_args():
     #     help="Path to imagenet val set for conducting zero shot evaluation.",
     # )
     parser.add_argument(
-        "--img-batch-size", type=int, default=64, help="Image batch size."
+        "--img-batch-size", type=int, default=1, help="Image batch size."
     )    
     parser.add_argument(
         "--context-length", 
         type=int, 
-        default=52,
+        default=512,
         help="The maximum length of input text (include [CLS] & [SEP] tokens)."
     )
     parser.add_argument(
@@ -102,8 +106,6 @@ def parse_args():
     args = parser.parse_args()
 
     return args
-
-
 def zero_shot_classifier(model, classnames, templates, args):
     with torch.no_grad():
         zeroshot_weights = []
@@ -118,13 +120,11 @@ def zero_shot_classifier(model, classnames, templates, args):
         zeroshot_weights = torch.stack(zeroshot_weights, dim=1).to(args.gpu)
     return zeroshot_weights
 
-
 def accuracy(output, target, topk=(1,)):
     pred = output.topk(max(topk), 1, True, True)[1].t()
     correct = pred.eq(target.view(1, -1).expand_as(pred))
     return [float(correct[:k].reshape(-1).float().sum(0, keepdim=True).cpu().numpy()) for k in topk]
 
-
 def run(model, classifier, dataloader, args):
     total_logits = []
     total_targets = []
@@ -134,17 +134,15 @@ def run(model, classifier, dataloader, args):
             images = images.to(args.gpu)
             target = target.to(args.gpu)
             total_targets.append(target)
-
             # predict
             image_features = model(images, None)
             image_features /= image_features.norm(dim=-1, keepdim=True)
             logits = (100.0 * image_features @ classifier).softmax(dim=-1)
             total_logits.append(logits)
-
             # measure accuracy
             acc1, acc5 = accuracy(logits, target, topk=(1, 1))
             top1 += acc1
-            n += images.size(0)
+            n += images.size(0) 
 
     outputs = torch.cat(total_logits, dim=0)
     targets = torch.cat(total_targets, dim=0)
@@ -162,7 +160,6 @@ def run(model, classifier, dataloader, args):
 
     return top1, outputs
 
-
 if __name__ == "__main__":
     args = parse_args()
 
diff --git a/run_scripts/zeroshot_eval.sh b/run_scripts/zeroshot_eval.sh
index 85144d3..0a99086 100644
--- a/run_scripts/zeroshot_eval.sh
+++ b/run_scripts/zeroshot_eval.sh
@@ -12,12 +12,12 @@ export PYTHONPATH=${PYTHONPATH}:`pwd`/cn_clip
 
 path=${2}
 dataset=${3}
 datapath=${path}/datasets/${dataset}/test
 savedir=${path}/save_predictions
 vision_model=${4} # ViT-B-16
 text_model=${5}
 resume=${6}
 label_file=${path}/datasets/${dataset}/label_cn.txt
 index=${7:-}
 
 mkdir -p ${savedir}
@@ -28,7 +28,7 @@ python -u cn_clip/eval/zeroshot_evaluation.py \
     --save-dir=${savedir} \
     --dataset=${dataset} \
     --index=${index} \
-    --img-batch-size=64 \
+    --img-batch-size=1 \
     --resume=${resume} \
     --vision-model=${vision_model} \
     --text-model=${text_model}
